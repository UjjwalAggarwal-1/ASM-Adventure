.model tiny
.data
.code
.startup
; wont run as a file, use this command in the debugx directly
    F 0 L 70 55h 6ah 6ah 77h 61h 6ch 20h
    ; THIS IS A DOS-BOX COMMAND
    ; DONT ADD 'H' IN DOSBOX, THERE ITS ALL UNDERSTOOD AS HEX
.exit
end
